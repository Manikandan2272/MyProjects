import { ExtractcardnumberDirective } from './extractcardnumber.directive';

describe('ExtractcardnumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtractcardnumberDirective();
    expect(directive).toBeTruthy();
  });
});
