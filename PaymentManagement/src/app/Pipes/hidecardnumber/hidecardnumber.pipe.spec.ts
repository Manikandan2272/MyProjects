import { HidecardnumberPipe } from './hidecardnumber.pipe';

describe('HidecardnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new HidecardnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
